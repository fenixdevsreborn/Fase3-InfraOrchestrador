# /opt/fcg-fenix/paymentsapi/docker-compose.yml

services:
  app:
    image: ${ECR_REGISTRY}:${IMAGE_TAG}
    container_name: fcg-fenix-paymentsapi
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-fcg_payments}
    volumes:
      - payments_pgdata:/var/lib/postgresql/data
    # ALB/target group usam porta 80 na EC2.
    ports:
      - "80:8080"
    restart: unless-stopped

volumes:
  payments_pgdata:
