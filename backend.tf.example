# Copie para backend.tf e preencha com seus valores.
# Depois: terraform init -reconfigure

# terraform {
#   backend "s3" {
#     bucket         = "fcg-terraform-state-SEU-ACCOUNT-ID"
#     key            = "fcg-infra/terraform.tfstate"
#     region         = "us-east-1"
#     dynamodb_table = "fcg-terraform-locks"
#     encrypt        = true
#   }
# }

# Pré-requisitos (uma vez):
# 1. Criar bucket S3 com versionamento e encryption
# 2. Criar tabela DynamoDB com chave primária LockID (String)
